<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 32 32">
  <!-- Background circle with gradient fill -->
  <circle cx="16" cy="16" r="16" fill="url(#gradient)" />
  
  <!-- Gradient definition -->
  <defs>
    <linearGradient id="gradient" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#b46e7e" /> <!-- var(--primary) -->
      <stop offset="100%" stop-color="#704834" /> <!-- var(--secondary) -->
    </linearGradient>
  </defs>
  
  <!-- Stylized "S" for SalonAI -->
  <text x="16" y="22" font-family="Arial" font-size="20" font-weight="bold" text-anchor="middle" fill="white">S</text>
</svg> 